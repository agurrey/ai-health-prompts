---
phase: 02-profile-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ClientShell.tsx
  - app/src/app/history/page.tsx
autonomous: true
requirements: [PRF-01, PRF-05]

must_haves:
  truths:
    - "User sees Profile link in the navigation bar"
    - "User on the history page sees XP teaser and a link to full profile"
    - "User on history page sees enhanced streak info with freeze tokens"
  artifacts:
    - path: "app/src/components/ClientShell.tsx"
      provides: "Profile nav link in header navigation"
      contains: "profile"
    - path: "app/src/app/history/page.tsx"
      provides: "XP teaser section and link to /profile, enhanced streak display"
      contains: "/profile"
  key_links:
    - from: "app/src/components/ClientShell.tsx"
      to: "/profile"
      via: "Next.js Link component"
      pattern: "Link.*href.*profile"
    - from: "app/src/app/history/page.tsx"
      to: "/profile"
      via: "Next.js Link component"
      pattern: "Link.*href.*profile"
    - from: "app/src/app/history/page.tsx"
      to: "app/src/lib/storage.ts"
      via: "getXP, getFreezeTokens imports"
      pattern: "import.*getXP.*from.*storage"
---

<objective>
Add Profile navigation link to the global nav bar and enhance the history page with XP teaser, freeze token display, and a link to the full profile.

Purpose: Make the profile page discoverable from anywhere in the app and cross-promote it from the history page where users already check stats.
Output: Updated ClientShell.tsx with Profile nav link, updated history/page.tsx with XP and profile link.
</objective>

<execution_context>
@/home/agurrey/.claude/get-shit-done/workflows/execute-plan.md
@/home/agurrey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From app/src/components/ClientShell.tsx (current nav links):
```typescript
// Nav links in order: Program, History, Prompts, About, LanguageToggle
// Each is: <Link href="/path" className="hover:text-foreground transition-colors">{t('EN', 'ES')}</Link>
```

From app/src/app/history/page.tsx (current imports):
```typescript
import { getCompletedDates, getStreak, exportData, importData } from '@/lib/storage';
// Page has: Calendar, stats grid (4 cards), export/import buttons
```

From app/src/lib/storage.ts:
```typescript
export function getXP(): { xp: number; xpLevel: number };
export function getFreezeTokens(): number;
```

From app/src/lib/gamification.ts:
```typescript
export function xpForLevel(n: number): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profile nav link to ClientShell</name>
  <files>app/src/components/ClientShell.tsx</files>
  <action>
Read app/src/components/ClientShell.tsx first.

In the Nav component's `<div className="flex items-center gap-4 text-sm text-muted">`, add a Profile link BEFORE the About link (so nav order becomes: Program, History, Profile, Prompts, About, LanguageToggle).

Add this link:
```tsx
<Link href="/profile" className="hover:text-foreground transition-colors">
  {t('Profile', 'Perfil')}
</Link>
```

No other changes to ClientShell.tsx.
  </action>
  <verify>
    <automated>cd /home/agurrey/Projects/ai-health-prompts/app && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Profile link appears in the nav bar between History and Prompts, bilingual text, navigates to /profile</done>
</task>

<task type="auto">
  <name>Task 2: Enhance history page with XP teaser and profile link</name>
  <files>app/src/app/history/page.tsx</files>
  <action>
Read app/src/app/history/page.tsx first.

Add imports: `getXP, getFreezeTokens` to the existing storage import line. Add `import Link from 'next/link'`. Add `import { xpForLevel } from '@/lib/gamification'`.

Add state variables: `xpData` (initialized in the existing useEffect via `setXpData(getXP())`), `freezeTokens` (via `setFreezeTokens(getFreezeTokens())`).

Add these two enhancements:

1. **XP teaser section** -- Insert AFTER the calendar and BEFORE the stats grid. A compact card showing:
```tsx
<Link href="/profile" className="block border border-border rounded-lg p-4 bg-card hover:bg-zinc-800 transition-colors">
  <div className="flex items-center justify-between">
    <div>
      <span className="text-fuchsia-400 font-bold">Level {xpData.xpLevel}</span>
      <span className="text-muted text-sm ml-2">{xpData.xp} XP</span>
    </div>
    <span className="text-muted text-sm">{t('View Profile', 'Ver Perfil')} &rarr;</span>
  </div>
</Link>
```

2. **Enhanced streak cards** -- In the existing 4-card stats grid, update the "Current Streak" card to show freeze tokens below:
- Keep existing streak.current display
- Add below the "Current Streak" label: `{freezeTokens > 0 && <span className="text-blue-400 text-xs">{'❄️'.repeat(freezeTokens)}</span>}`

No changes to the Calendar, export/import buttons, or overall page structure.
  </action>
  <verify>
    <automated>cd /home/agurrey/Projects/ai-health-prompts/app && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>History page shows XP level teaser card linking to /profile, streak card shows freeze token snowflakes when tokens > 0, all new text bilingual, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Profile link visible in nav bar on all pages
- History page shows XP teaser card between calendar and stats
- Clicking XP teaser navigates to /profile
- Freeze tokens displayed on streak card when > 0
</verification>

<success_criteria>
- Profile nav link discoverable from every page (ClientShell is the global shell)
- History page cross-promotes the profile page with XP teaser
- Freeze tokens visible on history page streak card (supports PRF-05 discoverability)
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-page/02-02-SUMMARY.md`
</output>
